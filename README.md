# data-analytics-group-2



## setup 

if you have uv installed, you can use `uv sync` to setup the python environment

you can install uv at https://docs.astral.sh/uv/#installation

then connect vscode to the kernel inside of the .venv folder